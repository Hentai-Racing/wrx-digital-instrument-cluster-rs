import { ItemBox } from "@lib/core.slint";

export struct Indicator {
    use_required_positions: bool,
}

export global IndicatorPositions {
    in-out property <bool> disabled: false;
}

export component IndicatorContainer { }
