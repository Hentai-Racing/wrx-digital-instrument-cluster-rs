export struct IDataParameter {
	max_value: int,
	min_value: int,
	value: int,
	units: string,
}

export struct FDataParameter {
	max_value: float,
	min_value: float,
	value: float,
	units: string,
}

export struct MTGearParameter {
	value: int,
	display: string,
}

export enum SUnits {
	USCS,
	SI
}
